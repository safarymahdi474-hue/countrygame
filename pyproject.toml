[project]
name = "countrygame"
version = "0.1.0"
description = "Telegram nation/war strategy game bot (inspired by, not a copy of, OurangWorld)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
